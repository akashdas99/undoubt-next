import React from "react";

export default function PageLoader() {
  return (
    <div className="grow grid">
      <div className="spinner-loader w-full place-self-center bg-primary"></div>
    </div>
  );
}
